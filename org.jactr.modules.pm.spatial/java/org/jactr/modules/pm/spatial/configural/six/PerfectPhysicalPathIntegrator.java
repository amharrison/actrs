package org.jactr.modules.pm.spatial.configural.six;

/*
 * default logging
 */
import org.apache.commons.logging.Log;
import org.apache.commons.logging.LogFactory;
import org.jactr.modules.pm.spatial.configural.AbstractConfiguralModule;
import org.jactr.modules.pm.spatial.configural.pi.DefaultPathIntegrator;

public class PerfectPhysicalPathIntegrator extends DefaultPathIntegrator
{

  /**
   * Logger definition
   */
  static private final transient Log LOGGER = LogFactory
                                                .getLog(PerfectPhysicalPathIntegrator.class);

}
